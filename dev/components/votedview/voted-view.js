Component({
    /**
     * 组件的属性列表
     */
    properties: {
        leftText: String,
        leftPercent: Number,
        leftNum: String,
        rightText: String,
        rightPercent: Number,
        rightNum: String,
        userOption: Number,
        result: Number,
        leftTotal: Number,
        rightTotal: Number,
        seal: Boolean,
        end: Boolean,
        settle: Boolean
    },
  
    /**
     * 组件的初始数据
     */
    data: {
    },
  
    /**
     * 组件的方法列表
     */
    methods: {
    }
  })